import {Container} from "./style"
import backgroundL from "../../images/bg-desktop-light.jpg"

export function Header({props}){
  return(
    <Container backgroundL={backgroundL}>
        
    </Container>
  )
}